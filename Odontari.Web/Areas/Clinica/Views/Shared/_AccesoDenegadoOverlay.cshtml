@* Overlay de acceso denegado: fondo borroso + tarjeta centrada. Incluir cuando ViewBag.MostrarAccesoDenegado == true. *@
<div class="acceso-denegado-overlay" id="accesoDenegadoOverlay" role="dialog" aria-modal="true" aria-labelledby="acceso-denegado-titulo">
    <div class="acceso-denegado-overlay-backdrop"></div>
    <div class="acceso-denegado-overlay-card">
        <div class="acceso-denegado-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
        </div>
        <h1 id="acceso-denegado-titulo">Acceso denegado</h1>
        <p>No tiene autorización para acceder a esta sección. Si considera que debería tener acceso, contacte al administrador del sistema.</p>
        <a asp-area="Clinica" asp-controller="Home" asp-action="Index" class="btn-volver">
            <span aria-hidden="true">←</span> Volver al inicio
        </a>
    </div>
</div>
<style>
    .acceso-denegado-overlay {
        position: fixed;
        inset: 0;
        z-index: 9999;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 1rem;
        pointer-events: none;
    }
    .acceso-denegado-overlay-backdrop {
        position: absolute;
        inset: 0;
        background: rgba(0, 29, 57, 0.35);
        backdrop-filter: blur(10px);
        -webkit-backdrop-filter: blur(10px);
        pointer-events: auto;
    }
    .acceso-denegado-overlay-card {
        position: relative;
        max-width: 440px;
        width: 100%;
        text-align: center;
        background: #fff;
        border-radius: 16px;
        box-shadow: 0 8px 32px rgba(0, 29, 57, 0.2);
        border: 1px solid rgba(0, 29, 57, 0.08);
        padding: 2.5rem 2rem;
        pointer-events: auto;
    }
    .acceso-denegado-overlay .acceso-denegado-icon {
        width: 72px;
        height: 72px;
        margin: 0 auto 1.25rem;
        border-radius: 50%;
        background: rgba(220, 53, 69, 0.1);
        color: #c92a2a;
        display: flex;
        align-items: center;
        justify-content: center;
    }
    .acceso-denegado-overlay .acceso-denegado-icon svg { width: 38px; height: 38px; }
    .acceso-denegado-overlay-card h1 { font-size: 1.5rem; font-weight: 600; color: #1a252f; margin-bottom: 0.75rem; }
    .acceso-denegado-overlay-card p { color: #5a6c7d; font-size: 0.95rem; line-height: 1.5; margin-bottom: 1.5rem; }
    .acceso-denegado-overlay-card .btn-volver {
        display: inline-flex;
        align-items: center;
        gap: 0.4rem;
        padding: 0.6rem 1.25rem;
        border-radius: 8px;
        font-weight: 600;
        font-size: 0.95rem;
        text-decoration: none;
        background: #4E8EA2;
        color: #fff;
        border: none;
        transition: background 0.2s;
    }
    .acceso-denegado-overlay-card .btn-volver:hover { background: #3d7a8c; color: #fff; }
</style>
